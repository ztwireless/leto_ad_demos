<?xml version="1.0" encoding="utf-8"?>
<!-- GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.DefaultCompany.demo_unity" xmlns:tools="http://schemas.android.com/tools" android:installLocation="preferExternal">
  <supports-screens android:smallScreens="true" android:normalScreens="true" android:largeScreens="true" android:xlargeScreens="true" android:anyDensity="true" />
  <application
      android:name="com.mgc.letobox.happy.MyApp"
      android:networkSecurityConfig="@xml/leto_network_security_config"
      tools:replace="android:networkSecurityConfig"
      android:extractNativeLibs="true" android:label="@string/app_name" android:icon="@mipmap/app_icon">
    <meta-data android:name="unity.builder" android:value="\1253210" />
    <meta-data
        android:name="MGC_APPID"
        android:value="1001675" />
    <meta-data
        android:name="MGC_GAMEID"
        android:value="364948" />
    <meta-data
        android:name="MGC_TEST_ENV"
        android:value="false"/>

    <!-- 适配android N 的文件系统，authorities 中注意替换您的包名，包名+".fileprovider" -->
    <provider
        android:name="com.mgc.leto.game.base.provider.LetoFileProvider"
        android:authorities="${applicationId}.leto.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
      <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/leto_file_path" />
    </provider>

    <!-- GDT sdk config -->
    <provider
        android:name="android.support.v4.content.FileProvider"
        android:authorities="${applicationId}.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
      <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/gdt_file_path" />
    </provider>

    <!-- lightingbox sdk config -->
    <provider
        android:name="com.wannuosili.sdk.ad.component.WNFileProvider"
        android:authorities="${applicationId}.WNFileProvider"
        android:exported="false"
        android:grantUriPermissions="true">
      <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/wn_file_paths" />
    </provider>

    <!-- toutiao config start -->
    <provider
        android:name="com.bytedance.sdk.openadsdk.TTFileProvider"
        android:authorities="${applicationId}.TTFileProvider"
        android:exported="false"
        android:grantUriPermissions="true">
      <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/file_paths" />
    </provider>

    <provider
        android:name="com.bytedance.sdk.openadsdk.multipro.TTMultiProvider"
        android:authorities="${applicationId}.TTMultiProvider"
        android:exported="false" />

  </application>
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.INTERNET" />
</manifest>